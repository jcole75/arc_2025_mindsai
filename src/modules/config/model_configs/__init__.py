"""Per-model configuration modules for ARC models."""

from __future__ import annotations

# Modules in this package are imported dynamically by modules.config.models.
# Keep this file lightweight to avoid side-effects during config import.
